[package]
name = "llmoxide-secret"
description = "Locked, self-zeroing memory and a zeroing global allocator."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
